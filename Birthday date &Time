from datetime import datetime

a = input("Enter birthday date (yyyy-mm-dd): ")
b = input("Enter today's date (yyyy-mm-dd): ")

a1 = datetime.strptime(a, "%Y-%m-%d")
b1 = datetime.strptime(b, "%Y-%m-%d")

print("No of days between:", (b1 - a1).days)
